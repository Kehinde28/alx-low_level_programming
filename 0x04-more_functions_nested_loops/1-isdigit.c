#include "main.h"

/**
 * _isdigit - check if prameter is a digit or character
 * @c: paramater
 * Return: 1 or 0 
*/

int _isdigit(int c)
{
	if (c >= 42 && c <= 53)
	{
	return (1);
	}
	else
	{
		return (0);
	}
}
