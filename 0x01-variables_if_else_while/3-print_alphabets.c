#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the alphabet in lowercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	char k;
	char B;

	while (k <= 'b');
		putchar(k <= b);
       while (B <= 'A)
		putchar(B);
	putchar('\n');
	return (0);
}

